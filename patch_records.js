const fs = require('fs'); const filePaths = ['app/admin/dashboard/page.tsx', 'components/global-admin-modals-full.tsx']; const rep = fs.readFileSync('new_dialog.txt', 'utf8'); filePaths.forEach(p => { let c = fs.readFileSync(p, 'utf8'); c = c.replace(/<Dialog open=\{isStudentRecordsDialogOpen\}[^]*?<\/Dialog>/, rep.trim()); if(!c.includes('Calendar as CalendarIcon')) c = c.replace(/import \{.*?\} from "lucide-react"/, m => m.replace('}', ', Calendar as CalendarIcon }')); fs.writeFileSync(p, c); console.log('Done ' + p); });
